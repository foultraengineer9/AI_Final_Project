# Wordify — core application. These five are all the base app needs.
streamlit>=1.49
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
joblib>=1.3
wordfreq>=3.0

# Development
pytest>=7.0

# --- Optional: the research ensemble panel and 3-tier mode -------------------
# Needs Python 3.12 (TensorFlow has no 3.13+ build). Install separately:
#
#   pip install sentence-transformers xgboost spacy nltk syllables groq python-dotenv
#   python -m spacy download en_core_web_sm
#   python -c "import nltk; nltk.download('wordnet'); nltk.download('omw-1.4')"
#
# Then run with:  WORDIFY_NO_TF=1 streamlit run main_app.py
# The base app runs without any of these; the panel disables itself.
